import React, { FC } from 'react';
import { Container, Row, Col } from 'react-bootstrap';

const Account: FC = () => {
	return (
		<Container>
			<Row>
				<Col>
					<h1>Account</h1>
				</Col>
			</Row>
		</Container>
	);
};

export default Account;
